import React from 'react';
import { HeroSection, CategoryDisplay } from '../index';

const Home = () => {
  return (
    <div>
      <HeroSection />
      <CategoryDisplay />
    </div>
  );
};

export default Home;
